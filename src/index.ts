import { MoneySystem } from "./MoneySystem";
import { Currency } from "./types";

/*
const system = new MoneySystem();

const john = system.addUser();
const linda = system.addUser();

system.sendTransfer({
  senderId: john,
  recipentId: linda,
  amount: 20,
  currency: Currency.PLN,
});

console.log(john);
console.log(linda);

*/
